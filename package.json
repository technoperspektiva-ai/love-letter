{
  "name": "love-letter",
  "private": true,
  "version": "3.1.0",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/cloudflare-bootstrap.mjs",
    "deploy": "node scripts/cloudflare-bootstrap.mjs && wrangler deploy",
    "dev": "wrangler dev",
    "bootstrap": "node scripts/cloudflare-bootstrap.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
